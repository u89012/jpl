---
id: std-class
title: class
---

# `class`

Default class-level behavior is exposed through `Class`.

Current methods:

- `name()`
- `location()`
- `fields()`
- `functions()`
- `parents()`

`fromJson(value)` is added when [`json`](/std-json) is required.
