---
id: std-object
title: object
---

# `object`

Default instance behavior is exposed through `Object`.

Current methods:

- `publicFields()`
- `privateFields()`
- `protectedFields()`
- `s()`

`toJson()` is added when [`json`](/std-json) is required.
